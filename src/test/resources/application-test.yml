spring :
  data:
    mongodb:
      uri: mongodb://localhost:27017/admin
  session :
  storage-type : redis
  redis :
    host : localhost
    port : 6379
security:
  jwt:
    token:
      security-key: secret-key
      expiration-length: 3600000
server:
  port: 8080